# 개발 기록 · 다음 세션 인계

최종 정리: 2026-09-18 · v0.24. 이 파일은 현재 보드게임 웹 프로토타입의 재개 기준입니다. 과거 대화보다 최신 사용자 요청과 실제 저장소 변경을 우선합니다.

## 먼저 읽을 곳

1. `README.md`: 실행·공개 주소와 현재 기능.
2. `RULES.md`: 현재 적용 규칙. 폐기된 숫자는 여기로 되돌리지 않습니다.
3. `DEVELOPMENT.md`: 구조·개발 경위·작업 재개 지점.
4. `VERIFICATION.md`: 버전별 실제 검수 범위. 과거 통과가 새 변경의 검수는 아닙니다.
5. `balance/REPORT.md`: 자동 플레이 결과와 제안. **제안은 승인·적용된 규칙과 구분**합니다.

## 프로젝트와 배포

- 저장소: https://github.com/UzuDang2/time-to-dino-boardgame
- 플레이: https://uzudang2.github.io/time-to-dino-boardgame/
- 기존 `UzuDang2/time-to-dino` 디지털판과 별도 프로젝트입니다.
- 브랜치 `main`, Pages `/ (root)`, `.nojekyll`. 프레임워크·번들러·빌드 과정 없는 정적 HTML/CSS/JS입니다.
- `python3 server.py --open` 또는 `Start.command`로 실행합니다. 작업 중 로컬 포트는 8767을 사용했습니다. 서버가 정상이면 재시작하지 않습니다.
- 1~4인 같은 기기 순차 플레이. 온라인 동시 접속·계정 간 저장 동기화는 구현되지 않았습니다.
- 저장 키 `time-to-dino-table-v1`, 데이터 버전 2. 브라우저·도메인별 저장입니다. 로컬과 공개 사이트 간에는 저장 파일 내보내기/불러오기를 이용합니다.
- 배포는 필요한 변경과 검수를 마친 뒤 main에 커밋·push → 해당 SHA의 Pages 성공 → 공개 자산/실제 UI 확인 순서입니다. Git push와 Pages 반영 성공을 별개로 기록합니다.

## 다시 구현하면 안 되는 확정 규칙

| 분야 | 현재 기준 |
|---|---|
| 시작 | 12장 덱, 손패 4장에 탐험 최소 1장. 빈 가방·캠프만 공개. 시작/출발 딸기 지급 없음 |
| 턴 | 2행동. 필드 턴 종료 포만감 −1. 캠프에서는 감소 없음 |
| 채집 | 채집 카드 최대 3개/발각 +1, 집중채집 담을 수 있는 잔여 전부/+2. 최초 성공 수령 때 비용 |
| 흔적 지우기 | 전용 카드만 가능. 개인·공용 발각 −1, 첫 인쇄 자원 1개를 해당 지형에 추가. 임의 카드로 대체 불가 |
| 조리·조합 | 완성 음식/조합마다 카드 1장과 재료, **행동 무료**. 행동 0도 자기 턴 종료 전 가능. 건설 가공은 1행동 유지 |
| 음식 | 생고기 포만감 +1/체력 −1, 구운 고기 +2/+1, 생선 +1/구운 생선 +3. 허브 2층은 기본 합계 +1 |
| 가방 | 21칸 외곽·중앙 12칸 개방. 1레벨 위 3칸, 2레벨 양옆 6칸. 나뭇가지 1칸, 비상식량 2칸/포만감 +3 |
| 수령 | 일반 보상 넘침은 획득 지형에 남김. **조합 완성품**은 임시 공간에서 무료 수령, 공간 없어도 제작 가능 |
| 떨어뜨리기 | 무료. 다시 줍기는 반드시 채집 카드/행동. 아이템 ID·모양·내구도·음식 층 보존 |
| 사냥 | 한 라운드 3장 해결=1행동. 라운드 뒤 계속/중단 선택. 마지막 행동 전투는 보상 정리 후 자동 턴 종료 |
| 무기 | 실제 가방의 칼/창이 공격 2 카드로 추가. 쓴 무기의 내구도만 −1, 기본 공격 손패 4장 별도 |
| 사냥감 | 멧돼지 A→B→?, 노루/큰 거북 C→B→?. 공개 행동 카드의 첫 문자만 첫 ?에 사용 |
| 발각 | 개인 8, 공용 12. 증가/흔적 지우기 변화량을 양쪽에 독립 적용. 전원 개인 5 조건은 폐기 |
| 캠프 | 귀환자 HP8/포만감6/개인발각0 + 즉시 턴 종료. 일부 귀환은 공용 유지. **전원 캠프이면 공룡 제거·공용0** |
| 구조 | HP/포만감0: 전 아이템 현장 낙하, 캠프 구조·해당 턴 종료. 구조 횟수 1회당 최종 −2점 |
| 공룡 | 거대 공룡 전투 불가. 테이블 전체 라운드당 패턴 한 단계. 떠난 지형 버림, 사람이 남으면 마지막 떠날 때 제거 |
| 지형 순환 | 미사용 덱 우선. 고갈 시 버린 지형 섞기. 재공개는 새로운 자원/사냥감/이벤트 상태 |
| 금속 | 동굴 최초 공개 최소 1개. 캠프 돌2→금속1, 카드1/행동무료 |
| 건설 | 재료별 전용 모양·종류당 공용3개, 회전만/뒤집기 금지. 건물에 놓으면 고정, 자재 보관소만 재배치 |
| 공용 창고 | 물품6×4, 최초 기부 점유칸만큼 공헌 토큰. 3토큰으로 이벤트1장. 재기부/이벤트 보상 토큰 순환 차단 |
| 비행기 | 연결된60칸=강철48+엔진4+배터리6+냉각2. 전원 캠프 후 출발. 현재 30라운드 제한 |

## 구현 지도

| 파일 | 역할 |
|---|---|
| `engine.js` | 순수 규칙·상태, `newGame`, `act`, `migrateSave`, 제작/전투/가방/공룡/비행기 |
| `app.js` | 화면 조립·입력·UI 상태·저장·되돌리기, map/hand/actionPicker/sidebar/camp |
| `terrain-data.js` | 원본 지형 인쇄 자원/이벤트 데이터 |
| `art.js`, SVG 자산 | 캐주얼한 배경·토큰·미플·사냥감·비행기 |
| `cooking-ui.js` | 음식/장비/재료/비행기 조합 목록과 손패 선택; 비용 판정은 엔진 |
| `camp-supplies.js` | 공용 창고·야외주방 물/음식 보관 UI |
| `construction-drag.js`, `block-visuals.js` | 잡은 칸 기준 건설 드래그, 같은 조각 연결 외형 |
| `manual-transfer-motion.js` 등 motion 파일 | 실제 원본→목적지 자원 이동/전투 순차 연출 |
| `card-lanes.css`, `hunt-map.css` | 좌우 카드 구역, 사냥감 표식, 지형 덱·붕괴 표식 |
| `tests.cjs`, `camp-tests.cjs`, `cooking-tests.cjs`, `rules-v024-tests.cjs` | Node 규칙 회귀검수 |
| `browser-smoke.cjs` | 격리 Chrome/Playwright로 좌우 카드·행동0 조합·공룡·반응형 확인 |

`act`는 복제 상태에서 검증·수행 후 성공한 결과만 원본에 덮어씁니다. 잘못된 비용/드롭은 원본을 보존해야 합니다. UI에서 상태를 직접 고쳐 비용을 우회하지 않습니다. `beginPayment→offerPayment→commitPayment`로 실제 토큰을 지불하며, 중간 단계도 저장합니다.

조합 수령은 `loot`의 `source:'crafted'`로 구분합니다. 일반 보상처럼 바닥으로 보내면 안 됩니다. 지형 인쇄 정보 `resourcePlan`과 현재 재고 `resources`는 구분합니다. `baseResources`는 버린 지형 재공개의 원본이며 현재 채집 잔량을 덱에 저장하면 안 됩니다.

`terrainDiscard`는 버린 지형 카드, `trampled`는 붕괴 대기입니다. 공룡·플레이어/처리 중 상태가 위치를 참조하는 동안 타일을 제거하지 않습니다. 마지막 귀환의 공용 초기화는 라운드 공룡 행동보다 먼저 일어나야 합니다.

## 검수와 재개

```sh
node --test *tests.cjs
node --check engine.js
node --check app.js
```

브라우저 검수는 별도의 Playwright 환경에서 실행합니다. 사용자 브라우저의 게임 저장은 테스트로 덮어쓰지 않습니다.

```sh
# require('playwright')가 가능한 환경에서 실행
DINO_TEST_URL=http://127.0.0.1:8767/ node browser-smoke.cjs
# 설치 Chrome을 쓰면 CHROME_EXECUTABLE, 스크린샷 위치는 DINO_QA_DIR 지정
```

재개 시 git 상태와 최신 커밋을 먼저 확인하고 다른 세션의 미커밋 작업을 보존합니다. 규칙 변경은 비용·UI 설명·수첩·README/RULES·저장 이관·의미 있는 회귀검사를 함께 맞춥니다. 자동 저장 원본은 보존하고 migration의 반복 실행에서 보상·자원이 중복되지 않는지 확인합니다.

## 주요 개발 이력

| 버전 | 커밋 | 변화 |
|---|---|---|
| 0.16 | `7d4734e` | 연결형60칸 비행기, 재료별 전용14종/총42개 건설 조각 |
| 0.17 | `9daf375` | 가방 무기에서 전투 카드 생성 |
| 0.18 | `a737a27` | 시작·출발 산딸기 제거, 캠프 진입 안내 강화 |
| 0.19 | `1532979` | 멧돼지 A/B 순서, 생/구운 고기 체력 효과 |
| 0.20 | `bd45e33` | 전 아이템 낙하, 구조횟수×2 최종 감점 |
| 0.21 | `1de0fef` | 공용 창고·주방 보관·공헌 토큰·정확한 블록 드래그 |
| 0.22 | `5997964` | 전용 흔적 카드, 시작 탐험 보장, 연결 조각 외형 |
| 0.23 | `1357d27` | 사냥감 위치/남은HP, 행동0 손패 dim·입력 차단 |
| 0.24 | 이 문서가 포함된 커밋 | 좌우 카드 구역, 금속 조합/동굴 보장, 지형 버림·재순환, 전원 귀환 리셋, 조리·조합 행동 무료 |

## 미정·주의할 구분

- **일반 카드를 버려 자원1개 복구하는 수색**은 제안 단계입니다. 구현하지 않았으며 전용 흔적 지우기를 대체하지 않습니다.
- 4인 숙련자 40~60분은 새 목표입니다. 자동 플레이 10회는 규칙 엔진상 진행 진단이며 실제 사람의 숙련도·판단 시간 측정이 아닙니다. 결과/조정안은 `balance/REPORT.md`에 별도 기록합니다.
- 경쟁 승점, 기부 인센티브, 구조 −2, 30라운드 제한 등 수치는 실제 플레이테스트로 조정할 수 있습니다. 자동 봇의 승률을 사람의 승률로 표시하지 않습니다.
- 그림체는 실사보다 깔끔한 캐주얼 일러스트. UI는 큰 지도, 하단 가방/정보, 실제 카드·토큰의 드래그와 부드러운 연출을 우선합니다.
- 예전 인쇄용 사각37장과 육각 목업은 참고 자료입니다. 이 웹판은 이후 변경이 많아 2026-09-17 초기 인쇄 규칙을 최신 웹판 규칙으로 취급하지 않습니다.
